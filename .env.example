# Public frontend runtime configuration.
# Never put private secrets (API keys, passwords, service-role keys) in VITE_* variables.

# Laravel API base URL used by the production frontend.
VITE_API_BASE_URL="https://api.example.com"

# Keep true only for local/mock development. Set false in production after backend endpoints are ready.
VITE_USE_MOCK_API="true"
